name: Accessibility Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  a11y:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          
      - name: Install Axe CLI and Server
        run: npm install -g serve @axe-core/cli
        
      - name: Run Accessibility Tests
        run: |
          # Start static server at repo root so /static/* absolute paths work
          serve -l 8000 . &
          # Wait for server to boot
          sleep 3
          # Sync Chrome and ChromeDriver versions to fix version mismatch on runner
          npx -y browser-driver-manager install chrome
          # Run Axe tests and fail on serious/critical violations
          axe http://localhost:8000/static/index.html --exit
